name: Deploy Frontend to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: SafeRide/frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd SafeRide/frontend
          npm ci

      - name: Build
        run: |
          cd SafeRide/frontend
          npm run build
          # Copy additional files needed for GitHub Pages
          cp public/.nojekyll dist/ 2>/dev/null || echo ".nojekyll already exists"
          cp dist/index.html dist/404.html
        env:
          NODE_ENV: production

      - name: List build output (for debugging)
        run: |
          echo "Build output contents:"
          ls -la SafeRide/frontend/dist/
          echo "Checking index.html exists:"
          test -f SafeRide/frontend/dist/index.html && echo "✓ index.html found" || echo "✗ index.html missing"

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: SafeRide/frontend/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
